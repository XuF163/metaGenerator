/**
 * Weapon alias/abbr table (scaffold, minimal).
 *
 * Rationale:
 * - Huge nickname lists are highly volatile and add repo noise.
 * - Prefer generating aliases from upstream weapon list (or LLM) during `meta-gen gen` when needed.
 */
export const abbr = {}

export const alias = {}

