/**
 * Weapon buff table (scaffold, minimal).
 *
 * Keep scaffold lean: weapon affix buff definitions should be generated from upstream data
 * (and optionally LLM) during `meta-gen gen`, not pre-bundled here.
 */
export default function () {
  return {}
}

