/**
 * Weapon alias/abbr config (scaffold, minimal).
 *
 * Keep scaffold lean: aliases are high-churn and can be derived/extended at runtime.
 * The generator will still work with empty alias maps.
 */
export const abbr = {}

export const aliasCfg = {}

