/**
 * Weapon buff table (scaffold, minimal).
 *
 * Keep scaffold lean: per-lightcone buff definitions should be generated from upstream data
 * during `meta-gen gen`, not pre-bundled here.
 */
export default function () {
  return {}
}

