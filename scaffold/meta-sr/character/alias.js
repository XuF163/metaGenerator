/**
 * Character alias table (scaffold, minimal).
 *
 * This is intentionally kept small to avoid stale, hand-maintained mappings.
 * If you want richer aliases, generate them from upstream data (or LLM) during `meta-gen gen`.
 */
export const alias = {}

