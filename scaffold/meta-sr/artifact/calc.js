/**
 * Relic buff table (scaffold, minimal).
 *
 * Keep scaffold lean: real relic set buffs should be generated from upstream APIs during
 * `meta-gen gen` and will overwrite the output file.
 */

export default {}

