{
  "//": "Copy to config.json (gitignored) and edit as needed.",
  "baselineRoot": "plugins/miao-plugin/resources",
  "outputRoot": "temp/metaGenerator/.output",
  "games": ["gs", "sr"],
  "types": ["artifact", "character", "material", "weapon"],
  "network": {
    "//": "Optional proxy/mirror for all HTTP(S) downloads. Either a prefix or a template with placeholders.",
    "proxy": "",
    "//httpProxy": "可选：HTTP 代理（例如 Clash: http://127.0.0.1:10809）。与上面的 proxy(URL 重写/镜像)不同，这是传输层代理（HTTPS 走 CONNECT）。",
    "httpProxy": "",
    "userAgent": "metaGenerator"
  },
  "validate": {
    "strictExtra": false,
    "//strictSha": "可选：非 JSON 文件是否要求 sha256 完全一致（默认 false；图片/派生脚本仅告警不失败）。",
    "strictSha": false,
    "sampleFiles": 800,
    "seed": ""
  },
  "gen": {
    "force": false,
    "forceCache": false,
    "forceAssets": false,
    "//baselineOverlay": "可选：生成时把基线 meta 作为覆盖层（用于兼容性排查；默认 false，保持“纯 API 生成”）。",
    "baselineOverlay": false,
    "//llmCalc": "可选：是否在 `meta-gen gen` 阶段使用 LLM 生成角色 calc.js（默认 false，建议用 `meta-gen calc` 进行可续跑批量升级）",
    "llmCalc": false
  },
  "calc": {
    "//": "calc.js 生成渠道：llm（默认）或 upstream（追随上游项目 genshin-optimizer/hsr-optimizer 生成/修补）",
    "//channelOptions": "支持：llm / upstream / upstream-direct（非 LLM，上游脚本直出）",
    "channel": "llm",
    "upstream": {
      "//genshinOptimizerRoot": "可选：genshin-optimizer 子模块路径（默认 upstream/genshin-optimizer）",
      "genshinOptimizerRoot": "upstream/genshin-optimizer",
      "//hsrOptimizerRoot": "可选：hsr-optimizer 子模块路径（默认 upstream/hsr-optimizer）",
      "hsrOptimizerRoot": "upstream/hsr-optimizer",
      "//includeTeamBuffs": "可选：是否包含上游的队友/团队 Buff（默认 false，避免引入队伍状态导致面板回归漂移）",
      "includeTeamBuffs": false,
      "//preferUpstream": "可选：当上游推导出的 buff 与现有计划冲突时，是否优先采用上游（默认 true）",
      "preferUpstream": true
    }
  },
  "llm": {
    "enabled": false,
    "baseUrl": "https://api.openai.com",
    "apiKey": "",
    "apiKeyEnv": "META_LLM_API_KEY",
    "chatCompletionsPath": "/v1/chat/completions",
    "model": "",
    "timeoutMs": 30000,
    "retries": 2,
    "maxConcurrency": 1
  }
}
